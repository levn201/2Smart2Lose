@page
@model RegisterModel
@{
    ViewData["Title"] = "Registrierung";
}

<link rel="stylesheet" href="~/css/Admin/AUTH.css" asp-append-version="true" />

<header class="auth-header">
    <a href="/Index" class="auth-logo">2Smart2Lose</a>
</header>

<main class="auth-main">
    <div class="auth-inner">

        <h1 class="auth-title">Konto erstellen</h1>
        <p class="auth-subtitle">Registriere dich mit deiner E-Mail-Adresse.</p>

        <div class="auth-card">
            <form method="post">

                <div asp-validation-summary="All" class="auth-error"></div>

                <div class="auth-field">
                    <label asp-for="Input.Email" class="auth-label"></label>
                    <input asp-for="Input.Email" class="auth-input" placeholder="name@beispiel.de" />
                    <span asp-validation-for="Input.Email" class="auth-validation"></span>
                </div>

                <div class="auth-field">
                    <label asp-for="Input.Password" class="auth-label"></label>
                    <input asp-for="Input.Password" class="auth-input" placeholder="••••••••" />
                    <span asp-validation-for="Input.Password" class="auth-validation"></span>
                </div>

                <div class="auth-field">
                    <label asp-for="Input.ConfirmPassword" class="auth-label"></label>
                    <input asp-for="Input.ConfirmPassword" class="auth-input" placeholder="••••••••" />
                    <span asp-validation-for="Input.ConfirmPassword" class="auth-validation"></span>
                </div>

                <button type="submit" class="auth-btn">Registrieren</button>

            </form>

            <div class="auth-links">
                <a asp-page="/Account/Login">Bereits ein Konto? Jetzt anmelden</a>
            </div>
        </div>

    </div>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
